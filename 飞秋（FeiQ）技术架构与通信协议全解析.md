# 飞秋（FeiQ）技术架构与通信协议全解析

飞秋（FeiQ）是飞鸽传书（IPMsg）的Windows平台汉化增强版，主打局域网内即时通信与文件传输，其核心设计围绕“去中心化、轻量高效”展开，可快速部署于各类局域网环境，适配个人办公、团队协作等场景。本文档将从技术架构与通信协议两大维度，深度解析飞秋的核心设计逻辑与实现细节。

# 一、飞秋核心技术架构

飞秋采用去中心化P2P（对等网络）架构，无需依赖中心服务器中转数据，所有客户端节点地位对等，直接通过局域网进行交互。这种架构是其实现低延迟通信、快速部署的核心基础，整体可拆解为6大核心功能模块，各模块职责清晰、协同联动。

## 1.1 整体架构核心特性

- 无中心节点：不存在服务端组件，所有客户端节点自主完成好友发现、消息传输、文件交互等核心操作，降低部署与维护成本。

- 局域网优先：针对局域网环境深度优化，依赖广播/单播机制实现节点互通，不依赖公网网络，即使断网（仅保留局域网连通）也可正常使用。

- 轻量低耗：无复杂服务部署流程，客户端仅需本地安装运行，资源占用少，适配早期低配设备及各类Windows系统版本。

- 兼容性强：完全兼容IPMsg协议，可与运行IPMsg的Windows、Linux、macOS等跨平台节点无缝通信。

## 1.2 核心功能模块拆解

|模块名称|核心职责|关键实现细节|
|---|---|---|
|网络通信模块|底层核心传输模块，负责数据的发送与接收|监听默认UDP 2425端口；处理UDP广播/单播消息；辅助建立TCP连接用于文件数据传输；检测网络连通状态|
|消息处理模块|协议解析与消息封装、分发|解析飞秋/IPMsg协议格式；封装不同类型消息（上线、文本、文件请求等）；将消息分发至对应模块（如好友管理、UI交互）|
|好友管理模块|维护在线好友列表与节点状态|接收上线广播并更新好友列表；通过心跳包检测节点在线状态；超时未收到心跳则标记节点离线；管理好友分组与黑名单|
|存储模块|本地数据持久化存储|存储用户配置（用户名、头像、端口等）；保存聊天记录、文件传输历史；采用ini或自定义本地文件格式，无需数据库依赖|
|UI交互模块|用户可视化操作与信息展示|渲染好友列表与在线状态；展示聊天消息与文件传输进度；提供用户输入界面（消息、文件选择等）；触发各类操作指令|
|文件传输模块|文件传输全流程管控|处理文件请求/应答；实现文件分片传输与断点续传（部分版本支持）；控制信息走UDP，实际文件数据走TCP保证可靠性|
## 1.3 核心交互流程

飞秋核心功能的交互流程围绕“节点发现-消息通信-文件传输”展开，各模块协同完成：

1. 节点发现流程：客户端启动 → 网络通信模块发送上线广播 → 其他节点接收后通过消息处理模块解析 → 好友管理模块更新在线列表 → UI交互模块渲染展示。

2. 消息通信流程：用户输入消息 → UI交互模块提交至消息处理模块 → 封装为标准协议格式 → 网络通信模块发送（单播/广播） → 接收方网络通信模块接收 → 消息处理模块解析 → UI交互模块展示消息。

3. 文件传输流程：用户发起文件传输 → 文件传输模块封装请求信息 → 网络通信模块发送文件请求 → 接收方应答（同意/拒绝） → 同意则建立TCP连接 → 分片传输文件数据 → 传输完成后校验完整性。

## 1.4 关键功能技术架构拆解

基于核心模块，用户列表、发送消息、上线、离线消息、文件传输、状态同步六大核心功能通过多模块协同实现，其技术架构聚焦“模块联动效率”与“状态一致性”，具体拆解如下：

### 1.4.1 上线功能技术架构

上线功能是节点互通的基础，核心目标是快速向局域网内其他节点宣告自身状态并同步好友信息，由网络通信模块、消息处理模块、好友管理模块协同完成：

- **核心涉及模块及职责**：
- 网络通信模块：负责构造上线广播数据包，通过UDP协议向局域网广播地址（255.255.255.255）发送，同时监听其他节点的上线响应；
- 消息处理模块：封装上线消息为标准协议格式（版本号|上线包标识|发送方IP等固定字段），解析接收的其他节点上线消息，提取节点核心信息；
- 好友管理模块：接收消息处理模块解析后的节点信息，更新本地在线好友列表，标记节点状态为“在线”；
- UI交互模块：同步好友管理模块的列表数据，渲染新增在线好友条目。

- **技术实现细节**：
- 广播触发机制：客户端启动时自动触发上线广播，同时每30秒同步发送心跳包（简化版上线消息）维持在线状态；
- 重复节点过滤：好友管理模块通过“发送方IP+机器名”双重标识去重，避免同一节点重复添加；
- 双向发现保障：接收方收到上线广播后，消息处理模块自动触发“回传上线消息”逻辑，确保发送方与接收方双向添加好友。

### 1.4.2 用户列表功能技术架构

用户列表功能核心是“实时维护在线状态、精准展示好友信息”，由好友管理模块主导，联动存储模块、UI交互模块实现，架构重点在于“状态同步及时性”与“数据持久化可靠性”：

- **核心涉及模块及职责**：
- 好友管理模块：核心管控模块，负责在线状态维护（接收心跳包更新状态、超时标记离线）、好友分组管理、黑名单过滤；
- 存储模块：持久化存储好友基础信息（历史好友IP、机器名、备注名）、分组配置，避免客户端重启后好友列表丢失；
- 网络通信模块：传输心跳包、离线通知等状态相关消息，为好友管理模块提供状态判断依据；
- UI交互模块：展示好友列表（含在线状态标识、分组折叠/展开）、响应用户分组编辑操作，同步至好友管理模块。

- **技术实现细节**：
- 状态更新机制：好友管理模块实时监听心跳包，超过1分钟未收到某节点心跳则标记为“离线”，并通知UI模块更新状态标识（如灰色头像）；
- 数据存储格式：采用ini文件或轻量本地数据库存储好友信息，键值对格式为“好友IP-机器名=备注名|分组名|最后在线时间”，便于快速读取与修改；
- 列表渲染优化：UI模块采用“增量更新”策略，仅当好友状态或列表数据变化时刷新对应条目，减少资源占用；支持按“在线状态”“分组”“用户名”多维度排序，提升用户查找效率。

### 1.4.3 离线消息功能技术架构

离线消息功能核心是“保障消息不丢失”，实现目标节点离线时消息暂存，待节点上线后自动推送，由消息处理模块、存储模块、好友管理模块、网络通信模块协同实现：

- **核心涉及模块及职责**：
- 消息处理模块：判断目标节点状态（通过好友管理模块查询），若离线则触发离线消息缓存逻辑，接收方上线后负责消息解析与分发；
- 存储模块：持久化存储离线消息，按“发送时间+接收方IP+发送方IP”建立索引，便于后续查询与推送；
- 好友管理模块：向消息处理模块提供目标节点实时状态（在线/离线），节点上线时触发离线消息推送通知；
- 网络通信模块：接收方上线后，按索引顺序批量推送离线消息，推送完成后标记消息为“已推送”。

- **技术实现细节**：
- 离线判断逻辑：消息发送前，消息处理模块向好友管理模块查询目标节点状态，若目标节点未在在线列表且未收到最新心跳包，则判定为离线；
- 消息缓存格式：离线消息按“协议完整格式+发送时间戳+推送状态”存储，确保接收方上线后可直接按标准协议解析；
- 批量推送策略：接收方上线后，网络通信模块采用“分批推送+逐条确认”机制，避免单批推送消息过多导致网络拥堵，每推送一条等待接收方回执后再推送下一条；
- 消息清理机制：离线消息默认保留30天，超过有效期或接收方成功接收后，存储模块自动清理对应缓存，释放本地存储空间。

### 1.4.4 文件传输功能技术架构

文件传输功能是飞秋核心优势之一，核心目标是“高效、可靠传输大文件”，支持分片传输与断点续传，由文件传输模块、网络通信模块、消息处理模块、UI交互模块、存储模块协同实现：

- **核心涉及模块及职责**：
- 文件传输模块：核心管控模块，负责文件分片、分片编号分配、断点位置记录、文件完整性校验；
- 网络通信模块：控制信息（请求/应答）通过UDP传输，文件分片数据通过TCP传输，建立临时TCP连接并维护连接稳定性；
- 消息处理模块：封装文件传输相关控制消息（请求、应答、暂停、继续），解析接收的控制消息并分发至文件传输模块；
- UI交互模块：展示文件传输进度（百分比、剩余时间）、传输状态（等待接收、传输中、暂停、完成、失败），提供暂停/继续/取消传输操作入口；
- 存储模块：记录文件传输历史（文件名、大小、传输时间、状态）、断点续传位置（分片编号），避免传输中断后重复传输。

- **技术实现细节**：
- 分片传输机制：文件传输模块将大文件按固定大小（默认4KB或8KB）拆分成分片，为每个分片分配唯一编号，按编号顺序传输；接收方按编号重组分片，确保文件顺序正确；
- 断点续传实现：传输中断时，存储模块记录最后成功传输的分片编号；重新传输时，文件传输模块从该编号开始继续传输，无需重复传输已完成分片；
- 完整性校验：文件传输完成后，发送方计算整个文件的MD5值并发送给接收方，接收方重组文件后计算MD5值进行比对，一致则判定传输成功，不一致则触发重新传输缺失分片；
- 传输控制策略：支持并发传输（默认最多3个文件同时传输），可通过UI模块手动调整并发数；传输过程中实时监控网络带宽，动态调整分片发送速率，避免占用过多网络资源。

### 1.4.5 状态同步功能技术架构

状态同步功能用于实现用户状态（在线、离线、离开、忙碌）的实时同步，让好友及时知晓用户可交互状态，由好友管理模块、UI交互模块、消息处理模块、网络通信模块协同实现：

- **核心涉及模块及职责**：
- 好友管理模块：维护所有好友的状态信息，接收状态更新消息并同步至本地状态列表；
- UI交互模块：提供用户状态设置入口（离开、忙碌等），实时渲染好友状态标识（如忙碌状态显示红色感叹号）；
- 消息处理模块：封装状态更新消息（扩展包类型标识），解析接收的状态消息并提取状态信息；
- 网络通信模块：向局域网内所有在线好友广播状态更新消息，确保状态同步及时性。

- **技术实现细节**：
- 状态类型扩展：在基础在线/离线状态外，扩展“离开”“忙碌”“请勿打扰”三种状态，通过新增包类型标识（如0x0100代表离开、0x0200代表忙碌）实现协议兼容；
- 状态触发机制：用户手动设置状态时，UI交互模块触发状态更新流程；离开状态支持自动触发（客户端无操作超过5分钟），回归操作后自动恢复在线状态；
- 状态同步流程：用户设置状态 → 消息处理模块封装状态消息 → 网络通信模块广播至所有在线好友 → 好友节点接收后解析状态 → 好友管理模块更新状态列表 → UI模块渲染状态标识；
- 兼容性保障：对于不支持扩展状态的旧版飞秋/IPMsg节点，状态消息会降级为基础在线/离线消息，避免协议冲突。

- **技术实现细节**：
- 状态更新机制：好友管理模块实时监听心跳包，超过1分钟未收到某节点心跳则标记为“离线”，并通知UI模块更新状态标识；
- 数据存储格式：采用ini文件或轻量本地数据库存储好友信息，键值对格式为“好友IP-机器名=备注名|分组名”，便于快速读取与修改；
- 列表渲染优化：UI模块采用“增量更新”策略，仅当好友状态或列表数据变化时刷新对应条目，减少资源占用。

### 1.4.3 发送消息功能技术架构

发送消息功能核心是“高效、准确传输文本内容”，支持单播/群播模式，由UI交互模块、消息处理模块、网络通信模块协同实现，架构重点在于“协议封装规范性”与“传输可靠性”：

- **核心涉及模块及职责**：
- UI交互模块：接收用户输入的文本内容，获取目标发送好友（单播）或好友组（群播），将消息内容与目标信息提交至消息处理模块；
- 消息处理模块：封装文本消息为标准协议格式（补充文本内容为附加字段），根据发送模式（单播/群播）拆分目标节点列表，为每个目标节点生成独立消息包；
- 网络通信模块：根据消息处理模块的目标列表，单播时直接向目标节点IP的2425端口发送UDP消息；群播时向多个目标节点IP逐一发送或发送至子网广播地址；接收方的网络通信模块收到消息后，转发至消息处理模块解析，最终由UI模块展示；
- 存储模块：可选存储聊天记录，按“发送时间+发送方+接收方+消息内容”格式持久化，支持后续查询。

- **技术实现细节**：
- 消息包可靠性保障：采用“包编号唯一标识”机制，消息处理模块为每个消息包生成随机唯一包编号，避免消息重复接收；
- 大文本消息处理：当文本内容超过UDP单包传输上限（默认1472字节）时，消息处理模块自动拆分消息为多包，接收方按包编号重组；
- 发送状态反馈：网络通信模块发送消息后，向UI模块返回“发送成功/失败”状态（失败场景含网络不通、目标节点离线等）。

# 二、飞秋通信协议深度解析

飞秋协议继承并扩展了IPMsg协议，采用明文、结构化字符串封装，核心基于UDP传输（控制信息），TCP仅作为文件数据传输的补充。协议设计简洁直观，适配局域网内快速数据交互需求。

## 2.1 协议基础属性

|属性项|具体内容|设计目的|
|---|---|---|
|底层传输协议|控制信息（上线、文本、文件请求）：UDP；文件数据：TCP|UDP无连接特性降低延迟，适配轻量控制信息；TCP保证文件传输可靠性|
|默认通信端口|UDP 2425（核心固定端口）；TCP端口动态分配（或自定义范围）|固定UDP端口便于节点互通发现；动态TCP端口避免冲突|
|字符编码格式|默认GBK编码|专为中文环境优化，避免中文消息乱码问题|
|传输特性|无连接、明文传输（早期版本）、支持广播/单播|简化协议设计，提升传输效率；局域网环境默认相对安全|
## 2.2 通用协议格式

飞秋所有消息均采用统一的结构化字符串格式，以竖线（|）作为字段分隔符，基础结构固定，部分字段可根据消息类型灵活扩展或省略：

```Plain Text

版本号|包类型标识|发送方IP|发送方端口|发送方机器名|发送方用户名|附加字段1|附加字段2|...|附加字段N
```

## 2.3 核心字段详细解析

### 2.3.1 基础固定字段

基础固定字段为所有消息类型必填项，用于标识消息基础信息与发送方身份：

|字段名称|含义说明|示例|
|---|---|---|
|版本号|协议版本号，兼容IPMsg协议|1.0、2.0|
|包类型标识|整数型（十六进制/十进制均可），区分消息类型的核心标识|1（上线广播）、4（文本消息）|
|发送方IP|发送节点的局域网IPv4地址，用于接收方回传消息|192.168.1.100|
|发送方端口|发送节点的UDP监听端口，默认2425|2425|
|发送方机器名|发送节点的主机名，辅助识别节点|DESKTOP-ZHANGSAN|
|发送方用户名|用户自定义昵称，用于UI界面展示|张三-技术部|
|附加字段|随消息类型变化的业务字段，如文本内容、文件名等|项目例会通知、项目文档.docx|
### 2.3.2 核心包类型标识（关键）

包类型标识是区分消息用途的核心，不同标识对应不同的业务逻辑，常用十六进制与十进制对应关系及用途如下：

|十六进制|十进制|消息类型|核心用途|
|---|---|---|---|
|0x0001|1|上线广播消息|节点启动后宣告在线，触发其他节点更新好友列表|
|0x0002|2|离线通知消息|节点主动退出时告知其他节点，快速清理离线列表|
|0x0004|4|文本聊天消息|用于单播（一对一）或群播（多对多）文字消息传输|
|0x0020|32|文件请求消息|向目标节点发起文件传输请求，携带文件关键信息|
|0x0040|64|文件应答消息|回复文件请求，告知发送方同意/拒绝传输及TCP端口|
|0x0080|128|心跳包消息|定期发送维持在线状态，用于离线检测|
## 2.4 典型消息类型协议示例

以下为飞秋最常用的4类消息协议示例，结合实际场景拆解字段含义与交互逻辑：

### 2.4.1 上线广播消息（包类型：1）

- 用途：节点启动后，向局域网广播（目标IP：255.255.255.255）该消息，实现自动好友发现

- 示例格式：
        `1.0|1|192.168.1.100|2425|DESKTOP-ZHANGSAN|张三|`

- 字段拆解：版本1.0 | 上线包（1） | 发送IP：192.168.1.100 | 端口：2425 | 机器名：DESKTOP-ZHANGSAN | 用户名：张三 | 无附加字段

- 补充：其他节点接收后，会主动回传一条上线消息，实现双向好友发现与列表更新

### 2.4.2 文本聊天消息（包类型：4）

- 用途：向单个或多个好友发送文字消息

- 示例格式：
        `1.0|4|192.168.1.100|2425|DESKTOP-ZHANGSAN|张三|今天下午3点开项目例会|`

- 字段拆解：版本1.0 | 文本包（4） | 发送IP | 端口 | 机器名 | 用户名 | 附加字段（文本内容：今天下午3点开项目例会）

- 补充：单播时直接发送到目标IP的2425端口；群播时发送到子网广播地址或多个目标IP

### 2.4.3 文件请求消息（包类型：32）

- 用途：发起文件传输请求，向接收方告知文件关键信息供确认

- 示例格式：
`1.0|32|192.168.1.100|2425|DESKTOP-ZHANGSAN|张三|项目文档.docx|1024000|/D/工作/项目文档.docx|`

- 字段拆解：版本1.0 | 文件请求包（32） | 发送IP | 端口 | 机器名 | 用户名 | 附加字段1（文件名） | 附加字段2（文件大小：1024000字节） | 附加字段3（发送方本地路径）

### 2.4.4 文件应答消息（包类型：64）

- 用途：接收方回复文件请求，明确是否同意传输并告知TCP端口

- 示例格式（同意传输）：
        `1.0|64|192.168.1.101|2425|DESKTOP-LISI|李四|同意|8888|`

- 字段拆解：版本1.0 | 文件应答包（64） | 接收方IP | 端口 | 机器名 | 用户名 | 附加字段1（应答状态：同意） | 附加字段2（TCP传输端口：8888）

- 补充：发送方收到同意应答后，通过TCP连接接收方8888端口传输文件；拒绝时附加字段1为“拒绝”，无TCP端口字段

## 2.5 关键补充说明

1. 离线检测机制：节点默认每30秒发送一次心跳包（简化版上线广播），若接收方超过1分钟未收到某节点心跳包，则自动标记该节点离线并从好友列表移除。

2. 安全性说明：早期飞秋协议为明文传输，局域网内可通过Wireshark等抓包工具解析消息内容与文件信息；部分新版飞秋新增AES-256加密功能，可手动开启。

3. 断点续传实现：支持断点续传的版本，核心是通过记录已传输的文件分片位置与MD5校验值，重新连接后从断点处继续传输，避免大文件重复传输。

4. 跨网段通信：默认仅支持同一网段通信，若需跨网段（如192.168.1.x与192.168.2.x），需配置路由转发规则，确保UDP 2425端口数据包可跨网段传输。

# 三、核心总结

1. 架构层面：飞秋以去中心化P2P架构为核心，6大功能模块协同工作，无需中心服务器，实现局域网内轻量、快速部署与低延迟交互。

2. 协议层面：基于UDP 2425端口构建核心通信，TCP辅助文件传输；采用“|”分隔的明文字符串格式，GBK编码适配中文环境，兼容IPMsg协议保证跨平台互通。

3. 核心优势：架构简洁、部署成本低、兼容性强、文件传输高效，是局域网内即时通信与文件共享的高效工具；不足在于早期版本安全性较弱，默认仅支持同网段通信。
> （注：文档部分内容可能由 AI 生成）