# 飞秋（FeiQ）产品功能详细文档

# 1. 文档概述

## 1.1 文档目的

本文档全面梳理飞秋（FeiQ）的产品功能体系，明确各功能点的详细描述、优先级、依赖模块及验收标准，为产品规划、开发排期、测试用例设计及项目落地提供核心依据，确保各角色对产品功能认知一致。

## 1.2 适用范围

本文档适用于飞秋产品研发团队（开发、测试）、产品经理、项目管理人员，同时可作为客户对接、内部培训的参考资料。

## 1.3 文档版本信息

|版本号|编制日期|编制人|修订说明|审批人|
|---|---|---|---|---|
|V1.0|2026-01-05|-|首次编制，完整梳理飞秋全功能体系及落地标准|-|
# 2. 术语与定义

|术语/缩写|定义说明|
|---|---|
|P0/P1/P2（优先级）|P0：核心必选功能，无则产品无法满足基础使用场景；P1：重要可选功能，优化核心场景体验，提升产品竞争力；P2：优化扩展功能，适配特殊场景或提升用户体验细节|
|依赖模块|支撑该功能实现的核心技术模块，参考飞秋去中心化P2P架构中的六大核心模块（网络通信模块、消息处理模块、文件传输模块等）|
|验收标准|可量化、可验证的功能达标条件，用于测试环节判断功能是否符合产品要求|
|P2P去中心化架构|无中心服务器，所有客户端节点地位对等，直接通过UDP/TCP协议交互，实现即时通信与文件传输|
|IPMsg兼容|飞秋基于飞鸽传书（IPMsg）协议开发，可与IPMsg跨平台节点（Windows/Linux/macOS）无缝通信|
# 3. 技术架构

## 3.1 架构概述

飞秋（FeiQ）采用去中心化P2P（Peer-to-Peer）架构，无中心服务器依赖，所有客户端节点地位对等，通过局域网内直接通信实现消息交互与文件传输。核心架构以六大模块为支撑，各模块协同工作，确保高并发、低延迟、稳定可靠的局域网通信体验。架构设计遵循“轻量高效、跨平台兼容、可扩展”原则，适配不同规模局域网（小型办公网络至大型企业园区网络）的使用需求。

## 3.2 核心模块架构

飞秋架构由六大核心模块组成，各模块职责清晰、交互紧密，具体如下：

|模块名称|核心职责|核心技术|与其他模块交互|
|---|---|---|---|
|网络通信模块|负责UDP/TCP协议封装与解析、节点发现与连接管理、数据收发与传输控制|UDP广播/组播、TCP流式传输、端口监听与绑定、网络状态检测|为所有模块提供网络传输支撑，接收消息处理模块的传输请求，向好友管理模块同步节点状态|
|消息处理模块|负责消息格式解析与封装、消息加密/解密、消息分发与路由、离线消息管理|IPMsg协议兼容、AES-256加密算法、消息队列、序列化/反序列化|接收网络通信模块的消息数据，向UI交互模块推送消息展示指令，与存储模块协同管理消息记录|
|文件传输模块|负责文件分片与合并、传输进度监控、断点续传控制、文件完整性校验|TCP高速传输、分片传输（Chunked Transfer）、MD5完整性校验、缓冲区优化|依赖网络通信模块实现数据传输，向UI交互模块反馈传输进度，通过存储模块记录传输历史|
|好友管理模块|负责在线节点发现与维护、好友分组管理、黑名单/白名单控制、用户状态同步|局域网扫描、心跳检测机制、用户信息缓存、分组索引|接收网络通信模块的节点状态数据，向消息处理模块提供用户路由信息，与UI交互模块同步好友列表|
|UI交互模块|负责用户界面渲染、操作指令接收、交互反馈展示（消息提示、进度条、弹窗）|跨平台UI框架（如Qt）、事件驱动模型、界面布局引擎、多线程UI更新|接收用户操作指令并分发至对应业务模块（如消息、文件模块），展示各模块反馈的状态与数据|
|存储模块|负责聊天记录存储、传输历史记录、用户配置信息、离线消息缓存的持久化管理|本地数据库（SQLite）、文件系统存储、数据压缩、缓存策略|为消息处理模块、文件传输模块、好友管理模块提供数据持久化服务，支持数据查询与导出|
## 3.3 架构交互流程

核心交互流程分为三大类，确保各模块协同高效：

- 节点发现流程：网络通信模块通过UDP广播发送节点探测包 → 接收其他节点响应包 → 好友管理模块解析并录入节点信息 → UI交互模块更新在线列表

- 消息传输流程：用户通过UI交互模块发送消息 → 消息处理模块封装并加密消息 → 网络通信模块通过UDP/TCP发送至目标节点 → 目标节点网络通信模块接收消息 → 消息处理模块解密解析 → UI交互模块展示消息

- 文件传输流程：用户通过UI交互模块发起文件传输请求 → 文件传输模块对文件分片并生成传输任务 → 网络通信模块建立TCP传输连接 → 分块传输文件数据 → 接收方文件传输模块合并分片并校验完整性 → 双方UI交互模块同步传输进度

# 4. 通信协议

## 4.1 协议基础

飞秋基于飞鸽传书（IPMsg）协议开发，兼容IPMsg v2.0及以上版本，同时扩展了加密传输、大文件传输等功能。通信采用“UDP控制+TCP数据”的混合模式：UDP协议用于轻量级控制消息（节点发现、状态同步、传输指令）的传输，确保低延迟；TCP协议用于大批量数据（文本消息、文件数据）的传输，确保可靠性。

核心通信端口：默认UDP通信端口为2425（可配置），TCP文件传输端口范围为8000-9000（自动随机选择可用端口），所有端口支持用户自定义配置以适配特殊网络环境。

## 4.2 协议数据格式

所有通信消息均采用统一的结构化格式，兼容IPMsg协议的同时扩展了加密字段，格式如下（UTF-8编码）：

```text

版本号:包编号:发送方用户名:发送方机器名:消息类型:消息内容[+加密标识+加密密钥索引]
```

各字段说明：

|字段名称|字段说明|示例|
|---|---|---|
|版本号|协议版本，兼容IPMsg时为“1”，飞秋扩展版本为“2”|2|
|包编号|唯一标识消息包，用于消息去重和回执确认，由发送方随机生成|1688852369|
|发送方用户名|发送方的飞秋用户名（或备注名）|张三-技术部|
|发送方机器名|发送方设备的主机名，用于辅助识别节点|DESKTOP-ABC123|
|消息类型|标识消息用途，飞秋扩展了IPMsg的消息类型枚举|MSG_SEND（文本消息）、FILE_SEND（文件传输请求）、STATUS_ONLINE（上线通知）|
|消息内容|核心数据，文本消息为明文/加密文本，文件传输请求为文件元数据（文件名、大小、MD5等）|“项目需求文档.docx|1024000|d41d8cd98f00b204e9800998ecf8427e”|
|加密标识（扩展字段）|标识消息是否加密，“1”表示加密，“0”表示未加密|1|
|加密密钥索引（扩展字段）|加密密钥在本地密钥库中的索引，用于解密时匹配密钥|0|
## 4.3 核心消息类型

基于IPMsg协议扩展，飞秋支持以下核心消息类型，覆盖节点交互、消息通信、文件传输全场景：

|消息类型枚举|用途说明|传输协议|是否需要回执|
|---|---|---|---|
|STATUS_ONLINE|节点上线通知，向局域网广播自身在线状态|UDP|否|
|STATUS_OFFLINE|节点离线通知，主动向局域网广播离线状态|UDP|否|
|STATUS_HEARTBEAT|心跳检测消息，定期向好友节点发送以维持在线状态|UDP|否|
|MSG_SEND|文本消息发送，支持普通文本和表情符号|UDP（短消息）/TCP（长消息）|是|
|MSG_RECEIPT|消息接收回执，向发送方确认消息已成功接收|UDP|否|
|FILE_SEND_REQ|文件传输请求，携带文件元数据（名称、大小、MD5等）|UDP|是|
|FILE_SEND_RSP|文件传输响应，同意/拒绝接收文件，携带TCP传输端口|UDP|否|
|FILE_DATA|文件数据分片，携带分片索引、分片大小、分片数据|TCP|是（分片回执）|
|FILE_COMPLETE|文件传输完成通知，携带整体文件MD5校验值|TCP|是|
|FILE_PAUSE/RESUME|文件传输暂停/继续请求，携带当前传输进度|UDP|是|
## 4.4 加密传输机制

为保障通信安全性，飞秋支持基于AES-256算法的消息与文件传输加密，具体机制如下：

- 密钥管理：采用“预共享密钥+动态协商”模式，用户可手动设置共享密钥，或通过局域网安全协商生成临时密钥，密钥存储于本地加密密钥库，不进行网络传输

- 加密范围：支持对文本消息、文件传输控制信息、文件数据全量加密，用户可在设置中开启/关闭全局加密或针对特定好友加密

- 加密流程：消息处理模块接收原始数据 → 采用AES-256-CBC模式加密 → 拼接加密标识和密钥索引 → 封装为标准协议格式 → 通过网络模块传输；接收方反之，先解析协议字段，再匹配密钥解密

# 5. 详细功能清单

# 3. 详细功能清单

## 3.1 核心基础功能模块

模块定位：支撑产品运行的基础能力，基于去中心化P2P架构实现，是所有功能的核心前提，确保产品在局域网内无需服务器即可正常运行。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F01-01|自动在线用户发现|客户端启动后，自动扫描同一局域网内所有在线节点，无需用户手动添加好友，自动生成并更新实时在线用户列表|P0|网络通信模块、好友管理模块|1. 启动后3秒内完成同一网段（如192.168.1.x）在线节点扫描；2. 在线列表完整展示用户核心信息（IP地址、用户名、机器名）；3. 可正常发现并显示IPMsg节点，实现跨协议兼容|
|F01-02|在线状态实时更新|通过上线广播、离线通知、定期心跳检测三重机制，实时同步所有节点在线状态，自动清理离线节点，确保在线列表准确性|P0|网络通信模块、好友管理模块|1. 节点上线后1秒内，其在线状态同步至局域网内所有其他节点的列表；2. 节点主动离线时，1秒内广播离线通知，其他节点即时移除该用户；3. 心跳周期默认30秒（支持配置），离线节点未发送心跳超1分钟后自动移除；4. 在线状态标识清晰（在线/离线），无状态错乱|
|F01-03|跨平台兼容通信|完全兼容IPMsg协议，可与运行IPMsg的Windows、Linux、macOS等多系统节点实现文本消息互发、文件双向传输|P0|网络通信模块、消息处理模块|1. 飞秋节点与IPMsg节点互发文本消息无乱码、无丢包；2. 两者可双向传输任意格式文件，传输完成后文件可正常打开；3. 飞秋可识别IPMsg节点的在线状态，IPMsg也可识别飞秋节点|
|F01-04|无服务器去中心化运行|产品无后台服务进程，仅通过客户端实现节点间直接P2P通信，无需部署任何中心服务器，实现即装即用、快速部署|P0|全模块（无外部依赖）|1. 客户端安装完成后直接启动即可使用，无服务配置环节；2. 任务管理器中无后台服务进程，仅存在飞秋客户端进程；3. 断开公网、仅保留局域网连通时，所有核心功能（通信、传输）正常使用|
## 3.2 即时通讯核心功能模块

模块定位：产品核心业务功能之一，满足局域网内用户即时沟通需求，覆盖私聊、群聊、消息增强等场景，适配个人沟通与团队协作通知需求。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F02-01|一对一私聊（文本）|支持用户之间一对一发送、接收纯文本消息，消息实时回执，聊天记录自动本地保存，适配个人日常沟通场景|P0|网络通信模块、消息处理模块、UI交互模块、存储模块|1. 消息发送后300ms内对方可接收，无明显延迟；2. 接收方成功接收后，发送方显示“已送达”回执；3. 聊天记录自动保存至本地，支持按联系人分类查询；4. 支持GBK、UTF-8编码自适应，中文消息无乱码|
|F02-02|群组聊天|支持用户创建无服务器聊天室，邀请多个在线用户加入，实现多人实时互动、群消息广播，适配团队协作沟通场景|P1|网络通信模块、消息处理模块、好友管理模块、UI交互模块|1. 支持创建最多20人的聊天室，创建过程无卡顿；2. 群消息发送后，所有群成员1秒内接收，无丢包；3. 支持群成员邀请、踢除操作，操作后即时生效；4. 群聊记录单独保存，支持按群名称查询|
|F02-03|批量群发消息|支持用户选择“所有在线用户”或自定义分组进行消息群发，适配企业通知、团队公告等批量传达场景|P1|好友管理模块、消息处理模块、网络通信模块、UI交互模块|1. 支持选择单个或多个自定义分组进行群发，群发范围可精准选择；2. 群发对象≤50人时，消息无丢包、无重复发送；3. 群发完成后，显示各接收方的消息送达状态（成功/失败）；4. 失败时给出明确提示（如“对方离线”“网络异常”）|
|F02-04|离线消息缓存与推送|当接收方离线时，发送方消息自动缓存至本地，接收方上线后自动推送缓存消息，避免消息丢失|P1|消息处理模块、存储模块、网络通信模块|1. 单个接收方离线时，支持缓存最多100条消息；2. 接收方上线后5秒内，缓存消息自动批量推送；3. 推送的消息顺序与发送顺序一致，无乱序、无丢失；4. 消息推送完成后，接收方聊天记录完整显示|
|F02-05|表情符号发送|支持发送系统自带表情，同时支持用户导入、管理自定义表情包，兼容GIF动画表情，丰富沟通形式|P1|UI交互模块、消息处理模块、存储模块、网络通信模块|1. 自带不少于50个系统表情，分类清晰（如开心、生气、工作）；2. 支持导入GIF、PNG格式的自定义表情，单个表情大小≤1MB；3. 发送的表情在接收方正常显示，GIF表情可正常播放，无错乱；4. 支持自定义表情包分类管理（创建/删除/重命名）|
|F02-06|快捷截图发送|支持用户自定义快捷键，一键触发截图功能，截图后可进行简单编辑（标注文字、绘制箭头），编辑完成后直接发送给目标用户|P1|UI交互模块、网络通信模块、消息处理模块|1. 截图快捷键可自定义（默认Ctrl+Alt+A），支持冲突检测并提示；2. 截图范围可自由选择，支持全屏、区域截图；3. 截图后支持添加文字、箭头、矩形框等标注；4. 编辑完成后发送，接收方300ms内接收，截图清晰无模糊|
|F02-07|语音聊天|支持局域网内用户一对一语音通话，基于局域网优化传输链路，实现低延迟、高质量语音通信，适配远程协作沟通场景|P2|网络通信模块、消息处理模块、UI交互模块|1. 语音通话建立时间≤2秒，通话过程延迟≤300ms，无明显回声；2. 支持实时音量调节，调节后即时生效；3. 局域网稳定环境下（带宽≥10Mbps），通话无杂音、无断连；4. 支持主动挂断通话，挂断后双方即时断开连接|
|F02-08|消息加密传输|采用AES-256加密算法对文本消息、文件传输控制信息进行加密，防止局域网内抓包泄露敏感信息，提升通信安全性|P2|消息处理模块、网络通信模块、存储模块|1. 加密功能支持手动开启/关闭，开启后所有通信消息自动加密；2. 通过Wireshark等抓包工具捕获的加密消息，无法解析出明文；3. 加密、解密过程无明显延迟（≤100ms），不影响用户体验；4. 加密状态下，飞秋节点间通信正常，无兼容问题|
|F02-09|聊天记录导出与备份|支持用户按联系人、时间范围筛选聊天记录，导出为TXT或Excel格式，同时支持手动将聊天记录备份至本地指定文件夹|P1|存储模块、UI交互模块|1. 支持按“全部联系人”“单个联系人”筛选，时间范围支持自定义（如近1周、2026-01-01至2026-01-05）；2. 导出的记录完整无遗漏，包含消息内容、发送时间、发送方；3. 备份文件可正常打开查看，备份过程无数据丢失；4. 支持备份文件导入恢复聊天记录|
## 3.3 文件传输核心功能模块

模块定位：产品核心优势功能，专注于解决局域网内大文件、多文件、文件夹的高效传输需求，通过UDP控制+TCP数据传输的组合机制，实现高速、稳定传输。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F03-01|单个/多个文件传输|支持用户选择单个文件、多个文件进行批量传输，无需提前压缩打包，适配日常文件共享场景|P0|文件传输模块、网络通信模块、UI交互模块|1. 支持所有常见文件格式（文档、图片、视频、安装包等），无格式限制；2. 支持一次性选择最多100个文件批量传输；3. 局域网带宽100Mbps环境下，传输速度≥10MB/s；4. 传输完成后文件可正常打开，无损坏、无缺失|
|F03-02|文件夹传输|支持直接传输整个文件夹（包含子文件夹及内部所有文件），传输过程中保持原目录层级结构，无需手动整理|P1|文件传输模块、网络通信模块、UI交互模块|1. 传输后文件夹的目录结构与原文件完全一致，子文件夹层级（最多10层）无错乱；2. 文件夹内包含多种格式文件时，所有文件均能正常传输；3. 100Mbps局域网环境下，传输速度≥8MB/s；4. 支持传输空文件夹，传输后空文件夹正常创建|
|F03-03|超大文件传输（≥4GB）|支持4GB以上超大文件（如大型工程文件、高清视频）传输，无文件大小限制，通过分片传输机制避免内存溢出|P1|文件传输模块、网络通信模块、存储模块|1. 成功传输10GB大小的视频文件或工程文件，客户端无崩溃、无卡顿；2. 传输过程中内存占用≤500MB，不影响其他系统操作；3. 传输完成后文件完整性校验通过，可正常打开使用；4. 超大文件传输支持暂停/继续操作，操作后无数据丢失|
|F03-04|断点续传|当文件传输因网络断开、手动暂停等原因中断后，重新连接或恢复传输时，可从已传输的断点位置继续传输，无需重复传输已完成部分|P1|文件传输模块、存储模块、网络通信模块|1. 手动暂停后重新传输，自动从当前进度开始，无重复传输；2. 网络断开后（如拔掉网线），重新连接局域网后5秒内自动重试传输，重试3次失败后给出明确提示；3. 断点续传后文件完整性无问题，与完整传输文件一致；4. 支持查看断点位置，续传进度更新准确|
|F03-05|传输进度实时显示|发送方和接收方均能实时查看文件传输进度，包含已传大小/总大小、传输速度、剩余时间，同时支持暂停、继续、取消传输操作|P0|文件传输模块、UI交互模块、网络通信模块|1. 传输进度更新频率≤1秒/次，进度显示准确（误差≤1%）；2. 剩余时间计算误差≤10%，随传输速度动态调整；3. 暂停、继续、取消操作即时生效，无延迟；4. 取消传输后，已传输的临时文件自动清理（可配置保留）|
|F03-06|传输限速设置|支持用户自定义文件传输速度上限，避免传输过程占用过多局域网带宽，影响其他网络操作（如网页浏览、视频会议）|P2|文件传输模块、UI交互模块、网络通信模块|1. 限速范围支持1KB/s~100MB/s自由配置，单位可切换（KB/s、MB/s）；2. 实际传输速度不超过设置的上限，误差≤5%；3. 限速开启后，不影响其他网络应用的正常使用；4. 支持全局限速或单个传输任务单独限速|
|F03-07|文件传输记录管理|自动记录所有文件传输历史，包含文件名、文件大小、传输时间、传输方向（发送/接收）、传输状态（成功/失败/取消）、对方信息等，支持查询筛选|P1|存储模块、UI交互模块|1. 传输记录永久保存，支持手动删除单条或批量删除；2. 支持按文件名（模糊搜索）、传输时间、传输状态、对方用户名筛选；3. 每条记录信息完整，无遗漏（如传输时间精确到秒）；4. 支持将传输记录导出为TXT格式|
|F03-08|共享文件夹设置|支持用户将本地任意文件夹配置为共享文件夹，可授权指定用户分组访问，同时支持设置密码保护，防止未授权用户访问共享资源|P2|文件传输模块、用户与权限管理模块、存储模块、UI交互模块|1. 支持同时设置最多10个共享文件夹，共享状态可单独开启/关闭；2. 授权功能支持按分组选择“允许访问”“拒绝访问”，授权后即时生效；3. 密码保护开启后，访问方需输入正确密码才能查看共享文件；4. 支持设置共享文件夹的访问权限（仅查看、可下载、可上传）|
|F03-09|拖拽传输文件/文件夹|支持用户直接拖拽本地文件或文件夹到聊天窗口、用户头像上，自动发起传输请求，简化操作流程|P1|UI交互模块、文件传输模块、网络通信模块|1. 拖拽单个/多个文件、单个文件夹到聊天窗口或用户头像，均能自动发起传输；2. 拖拽后弹出传输确认窗口（可配置默认直接传输）；3. 拖拽过程无卡顿，支持中途取消拖拽；4. 传输发起后，后续流程与手动选择文件传输一致|
## 3.4 协同办公增强功能模块

模块定位：基于核心通信与传输功能扩展，满足团队协同办公需求，覆盖远程协助、远程文件管理、共享剪贴板等场景，提升团队协作效率。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F04-01|远程协助（屏幕共享+控制）|支持用户向其他节点发起远程协助请求，请求通过后可实现屏幕共享、远程桌面控制，适配IT运维、技术支持、远程培训等场景|P2|网络通信模块、UI交互模块、用户与权限管理模块|1. 远程协助请求发送后，接收方收到弹窗提示，支持“接受”“拒绝”操作；2. 屏幕共享延迟≤500ms，画面清晰无模糊、无卡顿；3. 远程控制操作响应≤300ms，鼠标、键盘操作准确传递；4. 支持权限切换（仅查看、可控制），支持实时结束远程协助|
|F04-02|远程文件管理|支持用户远程访问对方节点的文件系统，可直接查看、下载、上传文件，无需手动发起传输，适配跨设备文件访问场景|P2|文件传输模块、网络通信模块、UI交互模块、用户与权限管理模块|1. 远程访问需对方授权，授权后可浏览对方所有磁盘分区及文件夹；2. 远程下载、上传文件速度≥5MB/s，文件完整性无问题；3. 支持在远程文件系统中新建、删除文件夹，操作即时生效；4. 支持按文件名模糊搜索远程文件，搜索响应≤1秒|
|F04-03|共享剪贴板|支持局域网内节点间共享剪贴板内容，用户在本地复制文本、图片后，可直接在其他节点粘贴使用，简化跨设备数据交换流程|P1|网络通信模块、消息处理模块、UI交互模块|1. 支持文本（≤100KB）、图片（≤10MB）剪贴板内容共享；2. 复制后在其他节点粘贴延迟≤200ms，内容无丢失、无错乱；3. 支持手动开启/关闭共享功能，关闭后不影响本地剪贴板使用；4. 多节点同时共享时，剪贴板内容准确对应，无混淆|
|F04-04|日程提醒|支持用户设置个人或群组日程提醒，可按年、月、周、日等时间维度配置，到期后通过弹窗、声音等方式推送提醒，适配团队任务管理场景|P2|存储模块、UI交互模块、消息处理模块|1. 支持设置单次、重复（每日/每周/每月/每年）提醒，重复规则可自定义；2. 提醒方式支持弹窗+声音，声音可自定义选择；3. 群组日程创建后，可通知所有群成员，成员收到日程提醒；4. 日程支持编辑、删除、完成标记，操作后即时生效|
## 3.5 用户与权限管理功能模块

模块定位：实现用户个性化管理与细粒度权限控制，满足企业/团队的组织架构管理需求，同时保障用户通信、文件传输的安全性。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F05-01|好友分组管理|支持用户自定义好友分组（如“技术部”“财务部”“项目组”），可批量将用户移动到不同分组，适配企业组织架构管理|P1|好友管理模块、UI交互模块、存储模块|1. 支持创建最多10个自定义分组，分组名称可修改；2. 支持通过拖拽或批量选择方式移动用户到分组，操作即时生效；3. 支持删除分组（分组内用户自动归为“未分组”）；4. 在线列表支持按分组筛选显示，筛选后无遗漏|
|F05-02|好友备注名设置|支持用户为好友设置个性化备注名，替代默认的用户名，便于快速识别好友身份（如“张三-技术部-前端”）|P1|好友管理模块、存储模块、UI交互模块|1. 备注名支持最多20个字符（含中文、字母、数字）；2. 备注设置后，在线列表中优先显示备注名，鼠标hover时显示原用户名；3. 备注信息永久保存，除非手动修改/删除；4. 支持批量为好友设置备注名|
|F05-03|黑名单功能|支持用户将指定节点添加到黑名单，黑名单内用户无法向当前节点发送消息、发起文件传输、请求远程协助，同时当前节点在对方在线列表中隐藏|P1|好友管理模块、消息处理模块、文件传输模块、网络通信模块|1. 添加黑名单后，对方发送的消息、文件请求均被屏蔽，无任何提示；2. 黑名单用户的在线列表中无当前节点，无法发现当前节点；3. 支持从黑名单中移除用户，移除后恢复正常通信；4. 黑名单列表支持按用户名、IP搜索|
|F05-04|隐身模式|支持用户设置全局隐身或对特定分组隐身，隐身状态下不向目标节点发送上线广播，目标节点无法发现当前节点，但当前节点可正常查看对方在线状态、发送消息|P2|好友管理模块、网络通信模块、UI交互模块|1. 隐身模式支持“全局隐身”“对指定分组隐身”两种模式，切换即时生效；2. 隐身状态下，目标节点的在线列表中无当前节点；3. 当前节点可正常查看所有节点在线状态，发送消息、文件正常；4. 支持快速切换“隐身/在线”状态，切换后1秒内同步|
|F05-05|权限控制（细粒度）|支持用户对文件传输、远程协助、共享文件夹访问等功能设置细粒度权限，可按“所有人”“仅好友”“指定分组”“拒绝所有人”配置|P1|用户与权限管理模块、文件传输模块、网络通信模块|1. 权限设置支持按功能分类（文件传输、远程协助、共享访问），配置后即时生效；2. 对未授权用户的请求，自动拒绝并给出明确提示（如“对方拒绝接收你的文件”）；3. 支持按分组批量设置权限，减少重复操作；4. 权限设置记录永久保存，可随时查看/修改|
|F05-06|个人信息自定义|支持用户设置个性头像、个性签名、机器名，这些信息将展示给其他在线节点，便于好友识别|P1|好友管理模块、存储模块、网络通信模块、UI交互模块|1. 头像支持JPG、PNG格式，单个文件大小≤5MB，上传后自动压缩适配显示；2. 个性签名支持最多50个字符，支持特殊符号；3. 机器名可自定义修改，修改后1秒内同步到其他节点；4. 个人信息修改后，其他节点的在线列表中即时更新|
## 3.6 个性化与系统设置功能模块

模块定位：满足用户个性化使用需求与系统适配需求，通过自定义设置提升用户体验，适配不同系统环境与用户操作习惯。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F06-01|界面皮肤自定义|支持用户切换系统自带皮肤，同时支持导入第三方皮肤文件，可调整界面布局（如好友列表位置、聊天窗口大小），适配不同用户审美|P2|UI交互模块、存储模块|1. 自带不少于5套系统皮肤，分类清晰（如简约、商务、卡通）；2. 支持导入后缀为.feiqskin的第三方皮肤文件，导入后无界面错乱；3. 界面布局支持拖拽调整，调整后即时生效并保存；4. 皮肤切换过程无卡顿，切换后所有界面元素正常显示|
|F06-02|快捷键自定义|支持用户自定义常用功能的快捷键，如消息发送、快捷截图、好友查找、打开聊天窗口等，同时支持快捷键冲突检测|P1|UI交互模块、存储模块|1. 支持至少10个常用功能的快捷键配置（如消息发送默认Enter，可修改为Ctrl+Enter）；2. 配置过程中若出现快捷键冲突（如与系统快捷键重复），给出明确提示；3. 快捷键配置后即时生效，无需重启客户端；4. 支持恢复默认快捷键设置|
|F06-03|网络参数配置|支持用户自定义UDP通信端口（默认2425）、TCP传输端口范围，适配特殊网络环境（如端口被占用、企业网络端口限制）|P2|网络通信模块、存储模块、UI交互模块|1. UDP端口支持1024~65535范围配置，输入非法端口时给出提示；2. TCP传输端口支持设置范围（如8000~9000），自动随机选择可用端口；3. 端口修改后，重启客户端生效；4. 端口配置错误（如端口被占用）时，启动时给出错误提示并引导修改|
|F06-04|多语言切换|支持简体中文、英文、繁体中文等多种语言切换，适配不同地区用户使用需求|P2|UI交互模块、存储模块|1. 支持至少3种语言（简体中文、英文、繁体中文）；2. 语言切换后，所有界面元素（菜单、按钮、提示语）均切换为目标语言，无乱码；3. 语言配置永久保存，重启客户端后不重置；4. 切换过程无卡顿，即时生效|
|F06-05|开机自启动|支持用户设置开机后自动启动飞秋客户端，无需手动打开，适配长期在线办公场景|P1|系统设置模块、存储模块|1. 开启自启动后，Windows系统开机3秒内飞秋自动运行，出现在系统托盘；2. 支持手动关闭自启动，关闭后开机不再自动运行；3. 自启动不影响系统开机速度（开机时间增加≤2秒）；4. 自启动后自动进入之前的登录状态，在线列表正常加载|
|F06-06|日志管理（系统/传输/消息）|支持用户查看、导出系统运行日志、文件传输日志、消息日志，日志包含时间、操作、状态等信息，便于问题排查|P2|存储模块、UI交互模块|1. 日志分类清晰（系统日志、传输日志、消息日志），每条日志包含时间（精确到秒）、操作内容、状态（成功/失败）；2. 支持按时间范围、日志类型筛选日志；3. 支持将筛选后的日志导出为TXT格式，导出完整无遗漏；4. 日志自动保留最近3个月记录，支持手动清理旧日志|
## 3.7 高级辅助功能模块

模块定位：为用户提供额外的辅助工具，提升产品使用效率，适配特殊场景（如跨网段通信、批量管理）与问题排查需求。

|功能点ID|功能名称|详细描述|优先级|依赖模块|验收标准|
|---|---|---|---|---|---|
|F07-01|在线用户搜索|支持用户通过用户名、IP地址、机器名等关键词快速查找局域网内在线用户，适配用户数量较多的场景|P1|好友管理模块、UI交互模块|1. 搜索支持模糊匹配（如输入“张”可匹配所有用户名含“张”的用户）；2. 搜索响应时间≤500ms，无明显延迟；3. 搜索结果高亮显示匹配的关键词；4. 支持按“用户名”“IP地址”“机器名”分类搜索|
|F07-02|网络诊断工具|内置网络诊断功能，可检测网络连通性、UDP 2425端口监听状态、目标节点可达性，生成诊断报告，帮助用户排查通信异常问题|P2|网络通信模块、UI交互模块|1. 支持检测UDP 2425端口是否被占用，给出占用进程信息；2. 支持ping测试目标节点IP，显示丢包率、延迟；3. 支持检测目标节点端口可达性（如TCP传输端口）；4. 诊断完成后生成报告，包含问题原因及解决方案建议|
|F07-03|批量操作（好友/文件）|支持批量添加好友、批量发送文件、批量设置用户权限等操作，减少重复工作，提升管理效率|P2|好友管理模块、文件传输模块、用户与权限管理模块、UI交互模块|1. 支持批量添加最多50个好友（通过导入IP列表）；2. 支持批量向最多50个用户发送同一文件/文件夹；3. 支持批量为最多50个用户设置相同权限；4. 批量操作过程中支持暂停/取消，操作完成后显示成功/失败统计|
|F07-04|API接口支持|提供基础HTTP API接口，支持对接企业内部管理系统，实现用户列表同步、消息自动推送、传输记录查询等自动化集成需求|P2|网络通信模块、消息处理模块、存储模块||
> （注：文档部分内容可能由 AI 生成）